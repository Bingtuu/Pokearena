# 027 · 赛事卡组管线设计 + CN mik 采集入库

| 项 | 内容 |
|---|---|
| 状态 | DOING（设计已落 PRD v1.8，采集实现进行中） |
| 关联 | PRD §FR-9 / §7.5（v1.8）；里程碑 M9-1 |
| 预估 | 2 天（含设计 0.5 天，已完成） |

## 背景（2026-08-02 三路调研实证）

- **CN = mik.moe 赛事 API（主源）**：端点全实测打通（`series-list → list →
  rank-individual → deck/detail` + `deck-static-by-tour` Meta 聚合），卡组卡标识
  = setCode+cardIndex 与本库主键一致，**零映射成本**；regulationMark/formatEnd
  直连合法性快照语境。详见 `docs/data-sources.md` §1。
- **EN = Limitless TCG（辅源，task 028）**：官方开放 API（匿名 50req/5min，
  standings 含完整 decklist：PTCGO set code + number + 英文名 → name_en 桥）；
  主站 HTML 收官方大赛上位；pairings 是逐局 matchup 胜率唯一可得源（Phase 4）。
- **JP = players.pokemon-card.com（壳源，后置）**：event_search /
  event_result_detail_search JSON 拿名次 + 官方卡组码；卡组内容需浏览器渲染
  （WAF 严格），壳先入、卡表渲染后置。
- 其他源评估（不采）：pokemon.cn 上位卡组专栏（图片非结构化，只做口径核对）、
  旅法师营地/B 站（截图/俗称，非结构化）、Trainer Hill（二手取 Limitless 上游）。

## 设计决定（已落 PRD v1.8）

- **范围约束（用户定）**：只入能映射到简中环境的卡组——CN 天然对应；EN/JP
  按映射率分档 mapping_status（full ≥95% / partial / unmapped），统计仅消费 full。
- **统计范围（用户定，硬约束）**：使用率/胜率统计仅 宝可梦/支援者/竞技场
  （stat_scope 派生过滤位）；能量/物品/宝可梦道具不进统计。粒度 = name_group。
- **保真**：deck_cards 全量 60 张落库（含能量），validate_deck 真值校验（task 026）
  与统计复用同一事实源；映射不上的卡 card_id 置空 + raw_name 保真，不猜。
- **胜率语义诚实声明**：mik 无逐局对阵（swiss 仅进行中）→ 代理指标 = 名次加权
  使用率 + top-cut 转化率；逐局 matchup 仅 Limitless pairings（EN）后置。
- **隐私最小化**：player_ref 只存官方选手编号 pinCode，不存昵称。

## 步骤

- [x] 三路数据源调研（EN/JP/CN）+ 文档化（docs/data-sources.md §1/§7/§8）
- [x] PRD v1.8：FR-9 + §7.5 三表 + 数据源矩阵 + 里程碑 M9
- [ ] migration：tournaments / decks / deck_cards 三表（user_version 3→4）
- [ ] mik 赛事采集器：series-list → list → rank-individual（top64）→ deck/detail
      入 raw（append-only + manifest）→ 三表入库；tier/division 开放词表
      `config/vocabularies/tournament_tiers.yml`
- [ ] stat_scope 派生（card_type/trainer_subtype 判定）+ 统计视图雏形
      （name_group × 赛事使用率，只消费 pokemon/supporter/stadium + full）
- [ ] TDD：端点 fixture 解析、入库对账（deck 60 张合计校验）、stat_scope 分类、
      统计视图口径
- [ ] 真实采集：近一赛季城市赛 + 最近一场超级赛/大师赛上位卡组（限速 2s）

## 验收标准

- [ ] 三表迁移幂等；采集 raw append-only + manifest 完整
- [ ] 入库卡组 deck_cards 合计 = 60 对账 100%（能量字母码归一化正确）
- [ ] 统计视图只含 pokemon/supporter/stadium 且仅 full 卡组；与 mik
      deck-static-by-tour 的 variant share 抽样对账一致（±1pp，口径差异注明）
- [ ] 全量测试绿、ruff 通过；STATUS/CHANGELOG 同步

## 完成总结（DONE 时填写）
