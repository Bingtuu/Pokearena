# 005 · M1-4 全量抓取 + 全量入库（draft）

| 项 | 内容 |
|---|---|
| 状态 | 进行中 |
| 关联 | PRD §4 Phase 1a、§7.1 对账口径、docs/mikmoe-api.md、goal（M1）限速红线与停止规则 |
| 预估 | 抓取 4~6 小时（后台）+ 形态调查与入库 0.5 天 |

## 目标

把 mik.moe 全系列卡牌抓到 raw 层并入库（status=draft），完成"字段形态再调查"，为 task 006（校验 draft→active）备齐数据。本任务不做 FR-2.3 校验实现与报告（在 task 006）。

## 步骤

- [ ] **抓系列清单**：`ptcgdb scrape sets`，确认全系列清单落 raw + sets 行（expected_count=cardsNum）；把系列总数、总卡数估计记入完成总结
- [ ] **全量抓卡**：`ptcgdb scrape cards`（缺省全系列；CSM1aC 已有 raw 自动跳过）。后台跑，中断/熔断后 resume 续传
- [ ] **字段形态再调查**：`tools/survey_fields.py` 扫全量 raw，对照 task 004 的 distinct 清单，找出 CSM1aC 没有的新取值（ex/太晶/ACE SPEC/V-UNION/训练家宝可梦/基本能量/新罕贵度/新能量码等）
- [ ] **词表补全（如需）**：新枚举核证后补词表（零猜测：必须有真实卡样本佐证；无法核证的一律 question，不猜映射）
- [ ] **全量入库**：逐系列 `ptcgdb ingest --set`，skipped 必须为 0；question 全量汇总记入完成总结
- [ ] **初步对账**：每系列 入库数 vs expected_count(+secret) 粗查，缺口系列与数量记入完成总结（正式对账判定在 task 006）

## 限速红线（goal 约束，违反即停）

- ≤1 次/2 秒；指数退避最多重试 3 次；连续 5 次失败或出现 403/验证码/非 JSON 立即熔断停机，不硬闯
- 熔断后只 resume 续传，不 `--force` 重抓已有数据

## 验收标准

- [ ] 全系列卡 raw 齐（三清单 missing 为空或缺口已定位原因）
- [ ] 全量入库 draft，skipped=0，question 全部如实列出
- [ ] 形态再调查结论完整：新枚举清单 + 核证依据 + 词表变更记录
- [ ] pytest / ruff 全绿（若本任务改了代码）

## 完成总结（DONE 时填写）

（待填：系列总数/总卡数、抓取耗时与中断记录、形态调查新发现、词表变更、question 汇总、初步对账缺口）
