# 025 · FR-3.4 同名计数引擎 + PRD v1.6

| 项 | 内容 |
|---|---|
| 状态 | TODO |
| 关联 | PRD §FR-3.4 / §6.2；里程碑 M7（validate_deck 前置） |
| 预估 | 1 天 |

## 目标

同名计数规则引擎落地：输入卡表按 name_group 规则计数（默认 4；ACE SPEC 全卡组 1；光辉 1；V-UNION 部件各 1）。先改 PRD v1.6：明确 `validate_deck` 的 Violation 语义全集（尤其 `evolution_chain` 的判定含义——PRD 目前只有枚举没有规则定义）。

## 步骤

- [ ] PRD v1.6：validate_deck 判定规则逐条定义（deck_size=60、name_limit、ace_spec_limit、banned、not_legal、evolution_chain 语义定死）、DeckReport 字段、与快照日期的关系
- [ ] 盘点 name_group 现有 deck_limit 字段覆盖率（A3 已校验 ACE SPEC=1），补齐光辉/V-UNION 规则数据源
- [ ] 计数引擎纯函数实现（供 CLI 与 SDK 双后端复用，对齐 legal engine 的纯函数核模式）
- [ ] TDD：跨插画同名（博士的研究/老大的指令）、owner 前缀不同名、ACE SPEC 标志不同名等 §6.2 全规则用例

## 验收标准

- [ ] PRD v1.6 落盘，violation 每种 kind 有形式化判定规则
- [ ] 计数引擎 §6.2 全规则用例通过；测试绿、ruff 通过

## 完成总结（DONE 时填写）
