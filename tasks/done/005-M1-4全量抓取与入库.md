# 005 · M1-4 全量抓取 + 全量入库（draft）

| 项 | 内容 |
|---|---|
| 状态 | DONE |
| 关联 | PRD §4 Phase 1a、§7.1 对账口径、docs/mikmoe-api.md、goal（M1）限速红线与停止规则 |
| 预估 | 抓取 4~6 小时（后台）+ 形态调查与入库 0.5 天 |

## 目标

把 mik.moe 全系列卡牌抓到 raw 层并入库（status=draft），完成"字段形态再调查"，为 task 006（校验 draft→active）备齐数据。本任务不做 FR-2.3 校验实现与报告（在 task 006）。

## 步骤

- [x] **抓系列清单**：`ptcgdb scrape sets`，确认全系列清单落 raw + sets 行（expected_count=cardsNum）；把系列总数、总卡数估计记入完成总结
- [x] **全量抓卡**：`ptcgdb scrape cards`（缺省全系列；CSM1aC 已有 raw 自动跳过）。后台跑，中断/熔断后 resume 续传
- [x] **字段形态再调查**：`tools/survey_fields.py` 扫全量 raw，对照 task 004 的 distinct 清单，找出 CSM1aC 没有的新取值（ex/太晶/ACE SPEC/V-UNION/训练家宝可梦/基本能量/新罕贵度/新能量码等）
- [x] **词表补全（如需）**：新枚举核证后补词表（零猜测：必须有真实卡样本佐证；无法核证的一律 question，不猜映射）
- [x] **全量入库**：逐系列 `ptcgdb ingest --set`，skipped 必须为 0；question 全量汇总记入完成总结
- [x] **初步对账**：每系列 入库数 vs expected_count(+secret) 粗查，缺口系列与数量记入完成总结（正式对账判定在 task 006）

## 限速红线（goal 约束，违反即停）

- ≤1 次/2 秒；指数退避最多重试 3 次；连续 5 次失败或出现 403/验证码/非 JSON 立即熔断停机，不硬闯
- 熔断后只 resume 续传，不 `--force` 重抓已有数据

## 验收标准

- [x] 全系列卡 raw 齐（三清单 missing 为空或缺口已定位原因）
- [x] 全量入库 draft，skipped=0，question 全部如实列出
- [x] 形态再调查结论完整：新枚举清单 + 核证依据 + 词表变更记录
- [x] pytest / ruff 全绿（若本任务改了代码）

## 完成总结（DONE 时填写）

**规模与对账（最终结论）**：129 系列全部入库 draft；cardsNum 目录合计 12,473，**按 `(卡条目自身 setCode, cardIndex)` 全局去重后 12,420 张唯一卡牌，DB 入库 12,420 张，对账 100% 通过、零缺口**。关键口径发现：mik 会把产品附赠的能量卡在多个系列的 cards 列表里重复列出，但这些条目的 `setCode` 指向其原生系列（如 CS1DC/CS3DC 列表里的 8 张基本能量 setCode='CSAC'，CSM2DC 列表里的 9 张 setCode='CSM1DC'，CSVH2C 列表里的 8 张 setCode='CSVH1C'）；ingest 按条目自身 setCode 归属入库，行为正确。**task 006 正式对账必须用去重 + 条目 setCode 口径**，不能按目录 cardsNum 直接比（目录口径会产生 15 个假缺口）。

**抓取执行**：run_id=20260801T052243Z-cef4c914，status=ok，fetched=12,262、skipped=211（task 004 已有小样）、question=0、missing=0，全程约 7 小时，限速 ≤1次/2秒，零熔断、零 403、零重试超标。

**形态再调查（全量 12,473 条目扫描）**：枚举面全部收敛、无未覆盖取值——mechanic ∈ {V, ex, GX, ACE SPEC, Prism Star, Radiant}；label ∈ {Single/Rapid/Fusion Strike, TAG TEAM, Ancient, Future, Ultra Beast}（含 ['Ultra Beast','TAG TEAM']、['Single','Rapid','Fusion'] 组合，逐项处理无碍）；cardType 6 种全覆盖；rarity 25 种全在词表；regulationMark A–J + 空串；pokemonAttr.stage 6 种全覆盖。**简中目前无太晶（Tera）卡样本**，is_tera 判定（名字/mechanic/label 含"太晶"）预置未触发，如实保留。

**本任务实测驱动修复（全部黄金样本 + 测试核证，64 测试全绿）**：
- V / VMAX / VSTAR / V-UNION / TAG TEAM GX / 战斗流派（一击·连击·汇流）——中途记录已有详述；
- **Radiant 光辉宝可梦**（CS5bC，rarity=K）→ rule_box_type=radiant（PRD 预置 prize=1/deck_limit=1）；
- **朱紫 ex**（SVP 梦幻ex 等，prize=2）+ **古代/未来 label**（轰鸣月ex/雄伟牙 等）；
- **ACE SPEC**（CSV7C，物品/特殊能量 mechanic）→ 新增 `NON_RULE_BOX_MECHANICS` 集合：非宝可梦 rule box，is_ace_spec=True、deck_limit=1 自动生效；罕贵度 `ACE` 补词表；
- **罕贵度补全**：无标记（构筑系列）、S/SSR、A（Amazing Rare）、宝石包符号 ●/◆/★/★★/★★★（CBB1C）、ACE；
- **eras 词表**：series='30th'（30thP 30周年庆典特典卡）→ 特典。

**question 全量汇总（均为解析器"不猜测、如实记录"的正确行为，无阻塞项）**：①系列内未解析到进化前卡牌（跨系列进化：特典包只收进化形态、宝石包复刻、化石宝可梦"古老的XX化石"）；②系列内赛制标记不唯一（如 "E,F"/"G,H,I"，sets 行存逗号连接值）；③特殊能量 provides 未结构化（条件性效果见 text_raw）。

**设计问题记录（PRD 未改，留待下次修订）**：①对账口径需写明"按条目 setCode + 全局去重"（上文）；②attacks 子结构 `cost_modifier` 增量字段（TAG TEAM GX "WWC+"）待 PRD §7.2 同步；③特典系列 product 级 setCode='PROMO' 与目录 setId 不一致，sets 行主键用目录 set_id（§7.1 set_id=商品编号口径的实测细化）。

## 中间记录（抓取进行中）

- **全量规模**：129 系列，cardsNum 合计 12,473 张（2026-08-01 scrape sets 实测）。
- **工具升级**：`tools/survey_fields.py` 支持全量扫描（传 `data/raw/mikmoe` 根目录即遍历全部系列）。
- **词表变更（已核证）**：rarities +`无标记`——CSM1DC 构筑系列实测，卡面无罕贵符号的普通卡（妙蛙种子等），先行扫描 729 张中发现 ×108。
- **编号外卡口径**：日月三个整系列（CSM1aC/bC/cC）全部编号在分母内，入库数==cardsNum；cardsNum 口径含 SR/UR。朱紫时代若有超分母编号，由对账规则兜住并在总结中记口径结论。
- **先行 ingest 验证**（draft，幂等）：CSM1bC 204 张、CSM1cC 212 张入库 skipped=0。新 question 类别：化石宝可梦 `evolves_from_text`（"古老的头盖化石"等）与系列内道具卡名（"谜之化石"）不对应——解析器不猜映射、记 question、原文保留，行为正确，如实入总结。
- **validate 端到端冒烟**：CSM1bC 真实数据六规则全过（204 张、抽样 11 张），报告渲染正常（冒烟报告已删，正式报告全量后生成）。
- **基本能量链路修复（CSM1DC 实测驱动）**：①基本能量 cardType 实测值 = `'Basic Energy'`（task 004 预置的 `'Energy'` 为猜测未实测，词表已更正并保留预置条目）；②provides 走卡名解析命中 9 种属性（含妖）；③`regulationMark=""` 统一存 NULL（迁移 002 放开 NOT NULL，user_version=2）；④规则 1 豁免基本能量的 regulation_mark/text_raw 非空（FR-3.2 + §7.2 依据，设计细节记录于此，PRD 未改）。修复后 CSM1DC 345/345 入库 skipped=0，validate 六规则全过（含 9 张基本能量）。
- **增量先行 ingest 第 2 批**：CSM1.5C 88 张 / CSMLC 5 张 / CSMYC 8 张 / SMP 63 张，skipped=0。新观察：赛制标记 `C` 首现（CSMYC，A/B/C 混合）；SMP 特典包 7 条"系列内未解析到进化前卡牌"question（跨系列进化，特典只收进化形态——解析器记 question 行为正确）。
- **特典系列 sets 行主键修复（SMP 实测驱动）**：mik 对特典系列 product 级 `setCode='PROMO'`（内部分组值），与目录名（setId）及卡级 setCode 不一致——涉及 SMP/SSP/SVP/30thP 四个系列。`_build_set_row` 改用目录 set_id 作主键（PRD §7.1 set_id=商品编号口径），否则 sets 行与 cards.set_id 错位、对账必炸。连带修复：mik 垃圾占位日期 `0001-01-01` → NULL（迁移 003 放开 sets.release_date NOT NULL，user_version=3）。
- **TAG TEAM GX 链路（CSM2aC/CSM2DC 实测驱动）**：①TAG TEAM GX 卡 = `mechanic="GX"` + `label=["TAG TEAM"]` → `rule_box_type=tag_team_gx`、prize_cards=3（PRD 预置口径），label 被 rule_box 消费不进 effect_tags；②cost 尾部 `"+"` = 追加费用标记（"WWC+"，追加能量触发追加效果），attacks 子结构新增 `cost_modifier` 增量字段（字段只加不删，与 damage_modifier 同构；PRD §7.2 attacks 描述未含此键，下次 PRD 修订时同步，记入总结）；非尾部 "+" 按未知形态抛错不猜测。修复后 CSM2aC 194/194、CSM2DC 366/366 入库 skipped=0。
- **编号外卡口径结论（对账用）**：mik `cardsNum` = **含编号外卡的全量数**（CSM2aC=194 = 编号内 171 + 编号外 SR 23 张，编号连续 172~194，number_display 分母即 194）。系列对账 = 入库数 == cardsNum 直接成立；`expected_secret_count` mik 无拆分数据留 NULL（卡面分母口径待 A2 验收与官方小程序核对）。卡级 `setCardsNum` 字段全为空串，未使用。
