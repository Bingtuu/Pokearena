# 024 · JP 映射填充与官方抽样核对

| 项 | 内容 |
|---|---|
| 状态 | DONE |
| 关联 | PRD §2.4（v1.5 → v1.6 修订）；里程碑 M6 收尾 |
| 预估 | 1 天 |

## 步骤

- [x] pokemon-tcg-data 卡数据（144 套，含补 svp/sve）+ PokéAPI 物种名表入 raw 层
- [x] EN TCGdex id → dexId 解析（套+编号匹配）；形态/机制词表
      `config/vocabularies/ja_name_rules.yml`
- [x] 填充 `name_ja`（宝可梦+基本能量）+ `external_ids(system='tcgdex')`；
      未覆盖（训练家/特殊能量/词表外形态）全量入 question 清单
- [x] 官方抽样核对：31 张分层样本，一致率修复后 100%（不符项全部有裁决）
- [x] PRD §2.4 修订（v1.6）+ M6 收尾

## 完成总结（DONE 时填写）

- **实现**：`ptcgdb/mapping/ja.py`（build_ja_name 组合器 + fill_ja 填充器 +
  fetch_ja_raw 采集器），CLI `ptcgdb map-ja [--fetch]`；测试
  `tests/test_mapping_ja.py` 4 测（零网络 fixture）。
- **填充结果**（reports/mapping-ja-20260802.md）：
  - `external_ids(system='tcgdex')` 落库 12,331 条（置信度 tcgdex-linked，
    = task 023 解析成功集合）；
  - `name_ja` 填充 **9,480 / 12,337 桥（76.8%）**，置信度 species-linked；
  - 冲突 0；question 清单 2,857 = trainer 2,715 + energy_special 125 +
    name_unmatched 16 + no_set_map 1。
- **官方抽样核对**（reports/official-check-ja-20260802.md）：31 张分层样本，
  修复前 26/31 = 83.9%，**修复后 31/31 = 100%**。不符项裁决：①シェイミ◇
  保留卡面符号（官方为图标 span）；②地区形态前缀加半角空格（426 张重填）；
  ③Bloodmoon/オーガポン面具为后缀型修饰（词表新增 suffix_modifiers 段 +
  build_ja_name 支持，オーガポン 41 张新填充）；④词表复审带出
  はくば/こくば 平假名修正（原误作漢字，28 张重填）。采样 32 请求走
  FetchURL 通道（本机 IP 被 WAF 限制），≤35 预算内。
- **TCGdex JA 本地交叉验证**：69.5%（受限于其 JA 库仅 SV 代起覆盖）。
- **ptcd 数据质量问题 2 例**（记录不猜）：Iono's Kilowattrel dexId=940 实为
  Wattrel；SVP-190/DRI-1055 桥值疑笔误（入 no_set_map 清单）。
- **导出契约不扩展的理由**：`name_ja`/`external_ids` 均不在七件套契约字段内
  （cards.jsonl 无 name_ja、external_ids 未列入导出面），dist/ 未变更，
  SDK 双后端模型未动，无需复跑导出。后续若要对外暴露多语言名，按 PRD
  字段只加不删规则另起任务。
- PRD 升 v1.6（§2.4 名字级 dexId 链）；pyproject ruff 排除 .scratch；
  .gitignore 挡 pcc-* 逆向中间产物（含 cookie）。
