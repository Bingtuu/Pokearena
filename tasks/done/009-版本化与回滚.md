# 009 · 版本化与回滚

| 项 | 内容 |
|---|---|
| 状态 | DONE |
| 关联 | PRD §6 FR-6（双轨版本化/回滚/冻结）、FR-3.3 冻结语义、FR-5.1 后处理；里程碑 M2；验收 A5/A6 |
| 预估 | 0.5 天 |

## 目标
落地双轨版本化（数据版本 vYYYYMMDD.N + schema_version SemVer）、快照 apply（关旧开新 + latest_text_overrides 刷新）、历史快照 override 冻结守卫、一键回滚（备份 DB 文件切换）。

## 步骤
- [ ] TDD：先写 `tests/test_versioning.py`（apply/冻结/回滚/版本递增）
- [ ] 实现 `ptcgdb/legal/versions.py`：meta 初始化、备份、apply_snapshot、update_text_overrides（冻结守卫）、rollback、CHANGELOG 四段式追加
- [ ] CLI `ptcgdb legal-apply --proposal` / `ptcgdb rollback`
- [ ] 真实库演练：模拟赛制变更提案 → apply → 验证 → rollback 还原

## 验收标准
- [ ] A5：模拟赛制页变更 → apply → 新快照生效；旧快照可查询（S5 历史回放）；历史快照 override 写入被拒绝（冻结）
- [ ] A6：脏合入后一键 rollback 至上一版本，数据无损
- [ ] 数据版本 vYYYYMMDD.N 同日递增；CHANGELOG.md 四段式追加；meta 表 schema_version 可读
- [ ] apply 自动刷新当前快照 latest_text_overrides（白名单旧卡 → 最新印刷）
- [ ] pytest 全绿（91 不回退）、ruff 通过

## 完成总结（DONE 时填写）

2026-08-01 完成。

- **做了什么**：`ptcgdb/legal/versions.py`——`apply_snapshot`（备份 → 闭合当前快照 → 写新快照 → 自动刷新 latest_text_overrides → 双轨版本号 → CHANGELOG 四段式追加）、`update_text_overrides`（历史快照冻结守卫 `FrozenSnapshotError`）、`rollback`（最新备份覆盖 DB）；CLI `legal-apply` / `rollback`。
- **验收结果**：8 个测试全过——A5（apply 后新快照生效、旧快照闭合可历史回放、旧快照 override 写入被拒）、A6（回滚后新快照消失、旧快照复原未闭合）、数据版本同日递增 .1→.2、CHANGELOG 四段式、latest_text_overrides 自动刷新且旧快照不回填；99 测试全绿、ruff 通过。
- **真实库演练**（DB 副本上进行，真实库未动）：模拟赛制变更提案（2026-09-16 加 J 标记）→ apply：新池按新快照判定、2026-08-01 历史回放仍命中 standard-2026-07-16（5,320 张不变）、白名单 overrides 自动刷新 90 条、meta 写入 schema_version=1.0.0/data_version=v20260801.1、冻结守卫拦截历史快照写入 → rollback：卡池复原 5,320、备份 ptcg-cn-base.db。
- **与预估的偏差**：无（0.5 天内）。
- **遗留问题**：真实库的首次 apply 会生成根目录 CHANGELOG.md（当前尚无真实变更，未生成）；data/versions/ 备份在 gitignore 的 data/ 下，不入库。

