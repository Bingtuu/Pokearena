# 011 · SDK 基础（双后端）

| 项 | 内容 |
|---|---|
| 状态 | DONE |
| 关联 | PRD §6 FR-8（下游 SDK）、FR-7；里程碑 M2；验收 A8 |
| 预估 | 1 天 |

## 目标
实现 `ptcgdb.sdk`：`open_db` / `open_jsonl` 双后端同一接口，返回 frozen Pydantic 模型，合法性为一等公民函数，规则语义双后端一致。

## 步骤
- [ ] legality.json 增 `errata` 键（additive，FR-6.2 允许）+ PRD §FR-7 一行同步——JSONL 后端的 effective_text 需要勘误数据
- [ ] 引擎抽纯函数核（快照选择/卡池构建/文本解析），session 版变薄适配层（既有 24 测试不动）
- [ ] TDD：先写 `tests/test_sdk.py`（接口行为 + A8 双后端一致性契约测试）
- [ ] 实现 `ptcgdb/sdk/`：CardDatabase ABC + DbBackend + JsonlBackend + open_db/open_jsonl
- [ ] 真实库 + 真实 dist/ 双后端一致性抽查

## 验收标准
- [ ] A8：open_db 与 open_jsonl 对同一查询集（get_card/search_cards/legal_at/effective_text/snapshots）返回一致（契约测试）
- [ ] 返回类型一律 frozen Pydantic，不暴露 ORM 对象；schema_version 可读（db 取 meta，jsonl 取 manifest）
- [ ] pytest 全绿（107 不回退）、ruff 通过

## 完成总结（DONE 时填写）

2026-08-01 完成。

- **做了什么**：
  - legality.json 增 `errata` 键（additive 扩展，PRD §FR-7 已同步一行）；
  - 引擎抽纯函数核 `select_snapshot`/`build_pool`/`resolve_text`（session 版变适配层，既有 24 测试零改动通过）；
  - `ptcgdb/sdk/__init__.py`：`CardDatabase` ABC + `DbBackend`/`JsonlBackend` + `open_db`/`open_jsonl`，接口含 schema_version/get_card/search_cards/get_set/list_sets/legal_at/effective_text/snapshots + 上下文管理器；
  - `apply_migrations` 幂等写入 `meta.schema_version`（FR-6.1 落地，SCHEMA_VERSION 统一自 migrations 模块）。
- **验收结果**：14 个 SDK 测试全过——A8 契约测试覆盖 get_card/search_cards（8 组查询）/sets/snapshots/legal_at（双赛制）/effective_text（含勘误、最新印刷路径）双后端全等；返回类型 frozen Pydantic 不暴露 ORM 断言通过；**真实库验证**：standard 5,320 / open 12,413 卡池双后端一致、search 喵喵 26 张一致、get_card/effective_text/snapshots 一致；121 测试全绿、ruff 通过。
- **实测修复**：apply_migrations 写入 meta.schema_version 与两个测试 fixture 手工种 meta 撞主键 → fixture 改为依赖迁移种子（22 个 fixture 错误已清零）；真实库经 `init-db` 补写 meta。
- **与预估的偏差**：无（1 天内）。
- **遗留问题**：`changelog(since)` 与 `validate_deck` 未实现——前者依赖真实 CHANGELOG 数据（首次 apply 后才有意义），后者 PRD 明确属 Phase 2；均未纳入本任务验收。

