# 008 · 合法性引擎

| 项 | 内容 |
|---|---|
| 状态 | DONE |
| 关联 | PRD §6 FR-3.1~3.3、§6.2；里程碑 M2；验收 A4 |
| 预估 | 1 天 |

## 目标
实现合法性引擎：`legal_at(date, format)` 按 FR-3.2 五步判定顺序输出合法卡池，`effective_text(card_id, date)` 按"勘误 > 最新印刷 > text_raw"解析文本，均为纯函数语义、返回 frozen Pydantic 模型。

## 步骤
- [ ] TDD：先写 `tests/test_legal_engine.py`，覆盖 A4 ≥12 组构造用例（合成库，不依赖真实数据）
- [ ] schemas 增加 `LegalityPool` / `EffectiveText` 模型（frozen）
- [ ] 实现 `ptcgdb/legal/engine.py`：快照选择（date+format）、五步判定、effective_text
- [ ] CLI `ptcgdb legal --date --format` 落地（替换 not implemented 桩）
- [ ] 真实库验证：`legal_at('2026-08-01', standard/open)` 卡池规模 + 关键卡抽查（妖能量双赛制、30thP 18 张、CSM2DC-339、博士的研究跨插画）

## 验收标准
- [ ] A4 构造用例 ≥12 组全过：博士的研究跨插画、ACE SPEC、owner 前缀、妖能量 standard 不合法/open 合法、视作B覆盖（CSM2DC-339 合法而同名其他印刷不合法）、开放 32 种白名单抽样、禁卡优先于一切（含禁卡>白名单、禁卡>赛制标记）
- [ ] 白名单按 name_group 匹配，返回该名下全部入库印刷行（FR-3.1 语义）
- [ ] effective_text 优先级：勘误（最新生效）> 最新印刷 > text_raw；历史快照 override 冻结不被本任务破坏
- [ ] pytest 全绿（67 不回退）、ruff 通过

## 完成总结（DONE 时填写）

2026-08-01 完成。

- **做了什么**：`ptcgdb/legal/engine.py`（快照选择 + FR-3.2 五步判定 + effective_text 三级解析）；schemas 新增 frozen 模型 `LegalityPool` / `EffectiveText`；CLI `ptcgdb legal --date --format` 落地。
- **验收结果**：24 组引擎用例全过（A4 要求的 ≥12 组全覆盖：博士的研究跨插画、ACE SPEC 与普通同名不同组、owner 前缀独立成组、妖能量 standard 不合法/open 合法、视作B 覆盖正反两例、开放白名单抽样、禁卡>赛制标记、禁卡>白名单、禁卡特性限定、快照缺失/未知赛制 LookupError、effective_text 勘误>最新印刷>原文 + 勘误生效日尊重）；白名单 name_group 语义（返回全部印刷行 + by_name_group）验证通过；91 测试全绿、ruff 通过。
- **真实库验证（2026-08-01）**：standard 卡池 5,320 张、open 12,413 张；5 张妖能量全不在 standard/全在 open；30thP 18 张全入 standard（白名单路径）；CSM2DC-339 不在 standard、在 open；open 排除恰好 7 张 = 禁卡表全部命中（阿塞萝拉 4 印刷、全满药 2 印刷、玛夏多破罐破摔 1 张——其他玛夏多保留，特性限定语义正确）；博士的研究 45 个印刷经同组规则全入池。
- **与预估的偏差**：无（1 天内）。
- **遗留问题**：`latest_text_overrides` 的自动刷新属 FR-5.1 后处理（task 009/M3 范围），当前快照该字段为空，effective_text 的 latest_print 路径已测但真实库暂无数据。

