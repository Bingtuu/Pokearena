# 012 · PRD v1.4 统一修订

| 项 | 内容 |
|---|---|
| 状态 | DONE |
| 关联 | PRD 全文（§2.1/§6 FR-2.3/§7.1/§7.2/A3）、AGENTS.md"代码与 PRD 同步"原则 |
| 预估 | 0.5 天 |

## 目标
把 M1/M2 实测发现的全部设计偏差一次性合并进 PRD，使权威文档与代码/schema 完全一致，为 M3/M4 提供准确事实来源。

## 步骤
- [ ] 合并 6 项偏差（来源：tasks/done/005、006、007、011）：
  1. 对账口径 = 条目自身 setCode + (setCode, cardIndex) 全局去重（§7.1/FR-2.3 规则 4 细化；附赠能量卡跨系列重复列出、目录口径产生 15 个假缺口的背景）
  2. attacks 子结构新增 `cost_modifier` 增量字段（TAG TEAM GX "WWC+" 实测，§7.2 JSON 示例同步）
  3. 特典系列 product 级 setCode='PROMO' 与目录 setId 不一致，sets 行主键用目录 set_id（§7.1 注明）
  4. FR-2.3 口径修订：规则 6 本期为 DB vs raw 同源自验（D1 后单源，降级源比对待 Phase 2）、规则 1 源数据缺失豁免先例（SSP-195）
  5. 简中暂无太晶卡样本（§2.1/A3 措辞对齐）
  6. 核对 M2 已并入项（开放白名单 32 种、legality.json errata 键）表述无遗漏
- [ ] 升文档版本 v1.4，修订记录完整列出合并项
- [ ] 逐条对照代码核验：orm/models.py、normalize（attacks/sets 主键）、validate（六规则）、export（legality.json）
- [ ] STATUS.md 同步（决策日志如有、入口已标 v1.4 则核对）
- [ ] pytest / ruff 全绿后归档提交

## 验收标准
- [ ] 6 项全部落地到 PRD 正文对应章节，修订记录完整
- [ ] PRD 描述与代码行为逐条一致，无残留偏差
- [ ] pytest 全绿、ruff 通过（文档任务不破坏测试）

## 完成总结（DONE 时填写）

2026-08-01 完成。

- **做了什么**：PRD v1.3→v1.4 统一修订，合并 7 项：①FR-2.3 对账 = 条目 setCode + (setCode, cardIndex) 全局去重（§7.1 expected_count/secret 口径同步）；②attacks 增 `cost_modifier`（§7.2 表格/JSON 示例/语义注释）；③§7.1 特典系列 PROMO setCode 与 sets 主键口径；④FR-2.3 规则 6 同源自验 + 规则 1 豁免（SSP-195、基本能量标记可空）；⑤太晶暂无样本（§2.1 补注 + A3 改补验）；⑥核对 M2 已并入项（32 种白名单、legality.json errata 键）；⑦全文清扫新发现的陈旧表述——D1 决策结果同步（§2.3 定位、FR-1.1/1.2 主源、§7.4 规模改实测 12,420/5,320/12,413、§8 架构图、风险登记册、第 14 章改决策记录）。
- **验收结果**：修订记录完整（v1.4 两段）；逐条对照 orm/models.py、normalize、validate、export 核验一致；121 测试全绿、ruff 通过。
- **越界修复（用户放行）**：一致性核验发现 `schemas/models.py` 的 `Attack` 模型缺 `cost_modifier` → 导出/SDK 静默丢字段（双后端契约测试因同经一个模型而未暴露）。按停止规则停下报告，**用户放行并入本任务**：`Attack.cost_modifier: str | None = None`（additive，兼容无键旧数据）+ 导出断言（test_export）。修复后导出 cards.jsonl 携带该字段。
- **与预估的偏差**：约 0.5 天，符合；中途一次停止-放行往返。
- **遗留问题**：A2 卡面核对（太晶数据形态、expected_secret_count 分母口径）依赖官方小程序，仍挂 Phase 2。

