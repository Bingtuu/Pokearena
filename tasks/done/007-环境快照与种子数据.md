# 007 · 环境快照与种子数据

| 项 | 内容 |
|---|---|
| 状态 | DONE |
| 关联 | PRD §6 FR-3、§7.3 legality_snapshots、§13 附录 A；里程碑 M2；验收 A4/A5 前置 |
| 预估 | 0.5 天 |

## 目标
把官方赛制页（2026-07-16 版）的 standard/open 双赛制合法性数据落成可版本化的种子文件并入库 `legality_snapshots`，为合法性引擎（task 008）提供数据底座。

## 步骤
- [ ] 核对官方赛制页，尽量补齐开放赛制 34 种白名单（PRD 附录 A 只点名 6/8 个新增）
- [ ] 写种子文件 `config/legality/standard-2026-07-16.yml`、`open-2026-07-16.yml`（标记集/能量种类/白名单/禁卡表/mark_overrides/生效期）
- [ ] TDD：先写 `tests/test_legal_seed.py`（种子完整性 + 入库幂等 + 字段一致）
- [ ] 实现 `ptcgdb/legal/seed.py`（yaml → Pydantic → upsert LegalitySnapshot，幂等）
- [ ] CLI `ptcgdb legal-seed`，对真实库执行并核对
- [ ] 真实库 sanity：白名单名称在库内有对应 name_group（缺口如实记录）

## 验收标准
- [ ] 种子文件计数与官方赛制页一致：standard = G/H/I + 8 能量 + 18 特典 + 26 旧卡；open = 全标记 + 9 能量（含妖）+ 18 特典 + 34 旧卡 + 3 禁卡
- [ ] mark_overrides 含天空之柱 CSM2DC-339→B（库内实际 card_id）
- [ ] `seed_snapshots` 重跑幂等（同 snapshot_id 不产生重复行）
- [ ] pytest 全绿（既有 64 不回退）、ruff 通过

## 完成总结（DONE 时填写）

2026-08-01 完成。

- **做了什么**：直连官方赛制页核对（一次只读请求，非采集管线）→ 种子文件 `config/legality/standard-2026-07-16.yml` / `open-2026-07-16.yml` → `ptcgdb/legal/seed.py`（Pydantic 种子模型 + upsert 幂等入库）→ CLI `ptcgdb legal-seed`；真实库已入库 2 条快照。
- **验收结果**：4 条全过——standard = G/H/I + 8 能量 + 44 白名单（18 特典 + 26 旧卡）+ 0 禁卡；open = A~I + 9 能量（含妖）+ 50 白名单（18 特典 + 32 旧卡）+ 3 禁卡（玛夏多/阿塞萝拉/全满药）；mark_overrides 含 CSM2DC-339→B；重跑幂等。白名单 94 个名称在库内 name_groups/cards 全部命中（missing=0）。
- **实测发现并修正**：
  - 开放赛制过去系列白名单实为 **32 种**（多出 6 种），PRD v1.3"34 种/8 种"系估计值——PRD 升 v1.4 同步修正（§2.1/FR-5.4/A1/A4/附录 A）。
  - 库内 J 标记 18 张 = 30thP 特典卡（即白名单 18 种本体），standard 下靠白名单路径合法；open 的 allowed_marks 定为 A~I（特典走白名单，与官方"全系列 + 以下列举"口径一致）。
  - 视作覆盖卡官方编号 CSM2D-339 在库内实际 card_id 为 `CSM2DC-339`（种子按库内口径登记并注释）。
- **与预估的偏差**：无（0.5 天内）。
- **遗留问题**：①官方"开放赛制可使用的特别的卡牌"外链清单未纳入 mark_overrides（yml 内 TODO 注明，待 M3 L1 监控补齐）；②PRD v1.4 另有 task 005/006 记录的待同步项（对账口径、attacks cost_modifier、PROMO setCode 等），尚未合并进 PRD，等统一修订。

