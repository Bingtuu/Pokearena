# 022 · EN 映射填充与交叉校验

| 项 | 内容 |
|---|---|
| 状态 | DONE |
| 关联 | PRD §2.4 / FR-2.3 规则 6 跨源比对（多源后补齐）；里程碑 M6 |
| 预估 | 1 天 |

## 目标

从已有 raw 提取英文桥字段，填充 `name_en` + `external_ids`；用 TCGdex / pokemon-tcg-data 静态数据交叉校验，产出覆盖率与不一致报告。全程纯本地（EN 源为 GitHub 静态 JSON 一次性下载入 raw 层），不碰 mik.moe。

## 步骤

- [x] raw 扫描：英文桥字段覆盖率统计（多少卡有 setCodeEn/nameEn）
- [x] 提取 → external_ids（system='mik-en'）+ name_en；无桥字段的卡记未映射清单
- [x] 交叉校验：name_en / TCGdex card ID（task 023 数据）对账；不一致清单（置信度降级或记 question）
- [x] 覆盖率 + 不一致报告落 reports/

## 验收标准

- [x] external_ids EN 覆盖率达到 raw 桥字段覆盖上限（缺口全部有清单）
- [x] TCGdex 交叉一致率写入报告，不一致项逐条有归类
- [x] 测试绿、ruff 通过；导出若含映射则双后端一致

## 完成总结（DONE 时填写）

**做了什么**：① raw 侦察：英文桥字段在单卡 `data` 顶层（nameEn/setCodeEn/cardIndexEn），12,473 raw 文件 99.3% 有桥；② 发现 **ingest 入库时已填充 name_en**（`ingest.py:115`，M1 起就有）——任务范围收敛为：覆盖核实 + `external_ids(mik_en)` 落库 + 报告；③ 新包 `ptcgdb/mapping/`（en.py 填充 + report.py 报告），CLI `ptcgdb map-en`；TDD 2 测试（填充/幂等，零网络 fixtures）。
**验收结果**：真实库 12,337/12,420 映射（99.3%）= raw 桥字段实际上限；无桥 83 张全部入清单（30thP 18、SSP 26、SVP 15、宝石包 14、SMP 6、散张 4——简中独占促销，符合预期不猜测）；external_ids mik_en 12,337 行、5,852 distinct EN 印刷（一 EN 多简中复刻）；报告 `reports/mapping-en-20260801.md`（含分系列覆盖表）；171 测试全绿、ruff 通过。
**与预估的偏差**：比预估小——name_en 已由 ingest 填充，工作量减半；置信度无需新列（system 枚举编码来源路径，PRD v1.5 口径），零 schema 变更。
**遗留问题**：TCGdex 交叉校验留 task 023；导出契约是否含 external_ids 留 task 024 决策；ORM 注释枚举已同步 v1.5。
