# 006 · M1-5 校验（FR-2.3）+ 校验报告 + draft→active

| 项 | 内容 |
|---|---|
| 状态 | DONE |
| 关联 | PRD FR-2.3（六条校验规则）、§7.1 对账口径、goal（M1）完成条件 ①② |
| 预估 | 0.5~1 天（与 task 005 后台抓取并行开发） |

## 目标

实现 `ptcgdb/validate/` 模块，落地 PRD FR-2.3 全部校验规则，产出校验报告；全部通过后把全量卡从 draft 置为 active。任一规则失败即阻断并出报告，不强行过关。

## FR-2.3 六条规则（逐条实现）

1. **必填非空**：卡名、卡号、赛制标记、卡牌种类、text_raw
2. **枚举合法**：属性、卡牌种类、罕贵度、赛制标记等在 `config/vocabularies/` 词表内
3. **能量成本合法且保序**：attacks/retreat_cost 的能量符号在能量词表内，顺序与 raw 一致（DB vs raw 复核）
4. **系列对账**：入库数 == `expected_count + expected_secret_count`（§7.1）。mik `cardsNum` 是否含编号外卡需用全量数据验证：若某系列入库数 > cardsNum，说明含编号外，把口径结论记入完成总结
5. **V-UNION 完整性**：union_part_of 关系 4 部件齐全、union_position 方位互斥不重复（无 V-UNION 样本的系列自动跳过该规则）
6. **抽样比对**：每系列 ≥5% 抽样（向上取整，至少 1 张），DB 记录 vs raw JSON 逐字段比对，卡名+HP+招式名一致率必须 100%
   - **与 PRD 的偏差说明**：PRD 原文为"与降级源抽样比对"，但 D1 后 M1 仅有 mik.moe 单源（官方小程序接口加密不可作降级源）。本期以 **DB vs raw 同源自验** 替代，报告中如实注明；降级源比对待多源（Phase 2）后补齐。此点记入完成总结，不改 PRD

## 步骤

- [x] `validate/rules.py`：六条规则实现，每条返回结构化结果（rule、passed、failures[]）
- [x] `validate/report.py`：校验报告生成（Markdown，落 `reports/` 目录，git 跟踪；含每规则通过率、失败明细、抽样清单）
- [x] CLI：`ptcgdb validate [--set SET_ID] [--report PATH]`；`ptcgdb activate [--set SET_ID]`（仅校验全过的系列可置 active，逐系列粒度）
- [x] 单元测试：合成 fixtures（必填缺失/枚举越界/能量乱序/对账缺口/V-UNION 缺件与方位重复/抽样不一致各至少 1 例），零网络
- [x] 全量数据到位后：跑全量校验 → 出报告 → 全过则 `activate` 全系列；不过的按 goal 停止规则停下报告

## 验收标准

- [x] 六条规则全部实现且有单测覆盖（正负例）
- [x] 全量校验报告产出，六条规则全过（或如实记录失败并阻断）
- [x] 全量卡 status=active（仅当全过）
- [x] pytest / ruff 全绿

## 完成总结（DONE 时填写）

**最终结论**：全量 129 系列 / 12,420 张卡六规则全过（`reports/validation-20260801T124141Z.md`，git 跟踪），全部 draft→active（12,420 张）。抽样规模 689 张（每系列 ≥5% 向上取整），卡名+HP+招式名一致率 100%。

**对账口径结论（落实 task 005 发现）**：规则 4 改为**条目自身 setCode + (setCode, cardIndex) 全局去重**口径——mik 把产品附赠能量卡在多系列列表重复列出（条目 setCode 指向原生系列），目录 cardsNum 口径会产生 15 个假缺口。去重口径下期望值 12,420 == 入库 12,420，129 系列 100% 通过。

**全量首跑暴露并已修复的 4 处问题**：
1. **规则 4 口径**：如上，改去重口径（测试 fixture 的 cards.json 同步补真实 cards 列表条目）；
2. **规则 3/6 raw 定位**：CSVH1C-DAR/FIG/FIR/WAT 4 张能量卡的权威 raw 在 CSVH2C 目录（跨目录归属），新增 `_raw_card_index` 全局 (setCode, cardIndex)→路径索引，规则 1/3/6 统一走索引；
3. **规则 5 V-UNION 分组**：CSEC 同名莫鲁贝可V-UNION 两组 8 件被星形关系连成一组——`union_relations` 修复为同名组内按 card_id 每 4 件切组（V-UNION 固定 4 部件），重 ingest CSEC 后 6 只组合体（24 部件）全部完整；
4. **规则 1 源数据缺失豁免**：SSP-195 洗翠的沉重球 mik 源 description 为空（单卡源数据缺口，非管线丢失）——DB 忠实反映源数据，规则 1 加"raw 同样为空则记 note 不判失败"豁免，报告中如实注明 `['SSP-195']`。

**与 PRD 的偏差记录（PRD 未改）**：①规则 6 原文"与降级源抽样比对"，M1 单源以 DB vs raw 同源自验替代（报告已注明，Phase 2 补齐）；②规则 4 期望值的实测口径（§7.1 细化）；③规则 1 源数据缺失豁免类别。
