# 010 · 导出七件套

| 项 | 内容 |
|---|---|
| 状态 | DONE |
| 关联 | PRD §6 FR-7（导出契约）、FR-6.1（manifest 双轨版本号）；里程碑 M2；验收 A7 |
| 预估 | 0.5 天 |

## 目标
实现 `dist/` 导出契约：manifest.json / cards.jsonl / sets.jsonl / relations.jsonl / legality.json / ptcg-cn.db / checksums.sha256 / schema.md，供下游离线消费。

## 步骤
- [ ] TDD：先写 `tests/test_export.py`（文件齐全、checksum 校验、JSONL 流式可读、legality.json 结构）
- [ ] 实现 `ptcgdb/export/exporter.py`：JSONL（Pydantic 模型序列化）+ legality.json（{meta, data} 结构）+ 只读 DB 副本 + checksums + schema.md（model_json_schema 半自动生成）
- [ ] CLI `ptcgdb export --out dist/` 落地（替换桩）
- [ ] 真实库导出到 dist/ 验证

## 验收标准
- [ ] A7：dist/ 文件按 PRD §FR-7 布局全部生成；checksums.sha256 重算校验通过；manifest.json 含双轨版本号（version + schema_version）与 counts
- [ ] cards.jsonl 一行一卡含 text_raw，可被下游流式读取（逐行 json.loads）
- [ ] legality.json 为 {meta, data: {snapshots: [...]}} 结构，含全部快照五类合法性数据
- [ ] pytest 全绿（99 不回退）、ruff 通过

## 完成总结（DONE 时填写）

2026-08-01 完成。

- **做了什么**：`ptcgdb/export/exporter.py`（序列化一律经 schemas Pydantic 模型，与 SDK 返回形状同源；WAL checkpoint 后复制 DB；checksums 不自签；schema.md 由 model_json_schema 半自动生成）+ CLI `ptcgdb export --out`。
- **验收结果**：8 个测试全过——文件齐全（PRD 布局 8 个文件）、manifest 双轨版本号+counts、checksums 重算校验、cards.jsonl 流式逐行读取且 text_raw 逐字保留（UTF-8 直写不转义）、legality.json {meta,data.snapshots} 结构、DB 副本 hash 一致、重跑幂等；107 测试全绿、ruff 通过。
- **真实导出验证**：`dist/` 生成——12,420 卡 / 129 系列 / 2 快照 / 21,818 关系；checksums 全部通过；version=v20260801.0（真实库 meta 暂无 data_version，走日期兜底；首次 apply 后自动带真版本号）。dist/ 在 gitignore，不入库。
- **与预估的偏差**：无（0.5 天内）。
- **遗留问题**：schema.md 目前只含 Card/Set/LegalitySnapshot 三模型字段表，SDK 返回模型（LegalityPool/EffectiveText）待 task 011 后可考虑补充；cards.parquet 为 Phase 4 可选项，本期不做。

