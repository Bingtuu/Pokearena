# 022 · EN 映射填充与交叉校验

| 项 | 内容 |
|---|---|
| 状态 | TODO |
| 关联 | PRD §2.4 / FR-2.3 规则 6 跨源比对（多源后补齐）；里程碑 M6 |
| 预估 | 1 天 |

## 目标

从已有 raw 提取英文桥字段，填充 `name_en` + `external_ids`；用 TCGdex / pokemon-tcg-data 静态数据交叉校验，产出覆盖率与不一致报告。全程纯本地（EN 源为 GitHub 静态 JSON 一次性下载入 raw 层），不碰 mik.moe。

## 步骤

- [ ] raw 扫描：英文桥字段覆盖率统计（多少卡有 setCodeEn/nameEn）
- [ ] 提取 → external_ids（system='mik-en'）+ name_en；无桥字段的卡记未映射清单
- [ ] 交叉校验：name_en / TCGdex card ID（task 023 数据）对账；不一致清单（置信度降级或记 question）
- [ ] 覆盖率 + 不一致报告落 reports/

## 验收标准

- [ ] external_ids EN 覆盖率达到 raw 桥字段覆盖上限（缺口全部有清单）
- [ ] TCGdex 交叉一致率写入报告，不一致项逐条有归类
- [ ] 测试绿、ruff 通过；导出若含映射则双后端一致

## 完成总结（DONE 时填写）
